# fly.toml app configuration file generated for basketball-tactic-board on 2026-06-03T04:33:07+03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'basketball-tactic-board'
primary_region = 'ams'

# Client-side observability — public identifiers (NOT secrets). Vite bakes these
# into the bundle at build time, so they need to be build args, not runtime secrets.
# To rotate, edit here and redeploy.
[build]
  [build.args]
    VITE_SENTRY_DSN = "https://1e225d21da9860669756fd9d4f3ee18e@o4511498792271872.ingest.de.sentry.io/4511498796269648"
    VITE_POSTHOG_KEY = ""
    VITE_POSTHOG_HOST = "https://us.i.posthog.com"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0

  [http_service.concurrency]
    type = 'connections'
    hard_limit = 25
    soft_limit = 20

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
